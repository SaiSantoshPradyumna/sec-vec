This si a Temp branch, which contaons Python starter code for working with Apache Kafka

to run this first install the following
```bash
pip install confluent-kafka
```

then run the 
```bash
python app.py
```
